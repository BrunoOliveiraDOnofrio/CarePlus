meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/funcionarios/login
  body: json
  auth: none
}

body:json {
  {
    "email": "marcos.ribeiro@clinica.com",
    "senha": "123456"
  }
}

script:post-response {
  if (res.status === 200) {
    bru.setVar("tokenId", res.body.token);
    bru.setVar("token", res.body.token);
  }
}
