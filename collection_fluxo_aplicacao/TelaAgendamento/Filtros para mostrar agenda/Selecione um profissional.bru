meta {
  name: Selecione um profissional
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/funcionarios/nomesPorEspecialidade
  body: json
  auth: bearer
}

auth:bearer {
  token: {{tokenId}}
}

body:json {
  {
    "especialidade": "Fonoaudiologia"
  }
}
